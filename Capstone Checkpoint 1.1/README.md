# Capstone Checkpoint 1.1 — Baseline (No Retrieval)

This folder contains the baseline checkpoint materials you can upload to your GitHub repository `Final-capstone-project` under `Capstone Checkpoint 1.1/`.

Files included:
- `capstone_checkpoint_1_1_baseline_starter.py` — main script
- `README.md` — this file
- `docs/MAINTAINING.md` — maintenance notes
- `requirements.txt` — dependencies

How to add this folder to your GitHub repo:

1. From the repository root (where `.git` exists), copy this folder into the repo:

```bash
cp -R "Capstone Checkpoint 1.1" "path/to/Final-capstone-project/Capstone Checkpoint 1.1"
```

2. Stage, commit, and push:

```bash
git add "Capstone Checkpoint 1.1"
git commit -m "Add Capstone Checkpoint 1.1 baseline materials"
git push origin main
```

If your default branch is `master` or another name, replace `main` appropriately.

If you want, I can prepare a patch or a branch ready for you to push — tell me whether you want help generating the exact `git` commands for your repo.
